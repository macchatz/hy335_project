relays ping and traceroute , and deciding the best route for client-server. Then images
are downloaded and some encryption between them is being used to transmit files. Basic encryption and threading for python.

For relay.py(We begin the relays first) --> 	python relay.py
For client -->	python client.py -e files\end_servers.txt -r files\relay_nodes.txt

Relays-files must be carefully selected
